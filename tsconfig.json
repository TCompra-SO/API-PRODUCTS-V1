{
  "compilerOptions": {
    "target": "es2016", // El objetivo del código compilado
    "module": "commonjs", // El sistema de módulos utilizado
    "esModuleInterop": true, // Compatibilidad con módulos CommonJS
    "outDir": "./dist", // Directorio de salida para los archivos .js
    "rootDir": "./src", // El directorio raíz donde están los archivos .ts
    "strict": true, // Habilitar las reglas estrictas de TypeScript
    "skipLibCheck": true, // Omitir la verificación de los archivos de las librerías
    "noImplicitAny": true // Prevenir que TypeScript asuma `any`
  },
  "include": ["src/**/*"], // Incluir todos los archivos .ts dentro de `src`
  "exclude": ["node_modules"] // Excluir `node_modules` para evitar compilación innecesaria
}